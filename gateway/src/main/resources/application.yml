server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: auth
              uri: http://auth:8080
              predicates:
                - Path=/auth/**

            - id: donation
              uri: http://donation:8081
              predicates:
                - Path=/donation/**

            - id: logistics
              uri: http://logistics:8082
              predicates:
                - Path=/logistics/**

            - id: needs
              uri: http://needs:8083
              predicates:
                - Path=/needs/**

          globalcors:
            cors-configurations:
              "[/**]":
                allowedOrigins:
                  - "*"
                allowedMethods:
                  - GET
                  - POST
                  - PUT
                  - DELETE
                  - PATCH
                allowedHeaders:
                  - "*"
                allowCredentials: false

jwt:
  secret: mi_clave_secreta_muy_segura_para_produccion
  expiration: 86400000
